namespace AudioAnalysis
{
    /// <summary>
    /// Used for personal visualization tests, not for the PLP algorithm.
    /// It's recommended to use unity AudioSettings class instead
    /// </summary>
    public static class FFTAudioAnalysisSettings
    {
        public static readonly int SampleRate = 48000;
        public static readonly int NumSamples = 1024;
    }
}